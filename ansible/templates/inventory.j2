{% for group, hosts in groups.items() %}
[{{ group }}]
{% for host in hosts %}
{{ host.name }} ansible_host={{ host.ip }} ansible_user={{ ssh_user }} ansible_ssh_private_key_file={{ keyfile }} {% if host.proxy %}ansible_ssh_common_args='-o ProxyJump={{ host.proxy }}'{% endif %}
{% endfor %}

{% endfor %}
